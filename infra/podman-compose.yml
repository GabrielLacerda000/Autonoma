version: "3.9"

services:
  redis:
    image: docker.io/redis:7
    container_name: content-engine-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data: